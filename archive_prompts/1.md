# MASTER PROMPT 1 --- Part 1/4

## Enterprise AI Gateway Implementation

> **NOTE TO CLAUDE**
>
> You are acting as the lead Staff AI Engineer responsible for
> implementing an enterprise-grade AI Gateway platform. This is not a
> prototype. Every implementation decision must prioritize
> maintainability, extensibility, correctness, observability, and
> production readiness.

# Primary Objective

Build the **foundation** of the AI Gateway project while preserving the
target architecture.

Do **not** redesign the architecture.

Do **not** simplify the implementation.

Do **not** introduce alternative frameworks unless there is a compelling
technical reason and you explain it.

Your implementation must remain compatible with future implementation
prompts.

------------------------------------------------------------------------

# Context (Authoritative)

The following architecture is the source of truth:

Client ↓ FastAPI API ↓ LangGraph Pipeline ↓ Redis / PostgreSQL ↓ Celery
Workers ↓ LLM Providers (LangChain)

Target request pipeline:

Input Validation → Prompt Rendering → Memory Injection → Input
Guardrails → Model Router → LLM → Output Guardrails → JSON Validation →
Memory Save → Logging

Never alter this execution order.

------------------------------------------------------------------------

# High-Level Deliverable

By the end of Prompt 1, the repository must contain:

-   Monorepo layout
-   Backend scaffold
-   Frontend scaffold
-   Docker Compose
-   PostgreSQL
-   Redis
-   FastAPI
-   SQLAlchemy
-   Alembic
-   JWT Authentication
-   Configuration system
-   Dependency Injection
-   Logging
-   Middleware
-   Base database schema
-   Health endpoints
-   API versioning
-   Repository pattern
-   Service layer skeleton
-   CI-ready project structure

The codebase must run locally using:

``` bash
docker compose up --build
```

without manual configuration beyond environment variables.

------------------------------------------------------------------------

# Repository Rules

Before writing code:

1.  Inspect the repository.
2.  Preserve existing code.
3.  Extend rather than rewrite.
4.  Never duplicate modules.
5.  Keep imports organized.
6.  Maintain type safety.
7.  Use Python type hints everywhere.

------------------------------------------------------------------------

# Required Folder Structure

``` text
ai-gateway/
│